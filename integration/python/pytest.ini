[pytest]
# Show wall-clock duration for every test (slowest first). Mirrors the
# per-test timing the other integration suites surface (vitest native, gradle
# afterTest listener, rspec --profile, go test -v `--- PASS (0.45s)`).
addopts = --durations=0 --durations-min=0
