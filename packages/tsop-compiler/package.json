{
  "name": "tsop-compiler",
  "version": "0.1.0",
  "type": "module",
  "description": "TSOP reference compiler (TypeScript): nanopass pipeline from TS to Bitcoin Script",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "files": ["dist"],
  "dependencies": {
    "ts-morph": "^24.0.0",
    "tsop-lang": "workspace:*",
    "tsop-ir-schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
