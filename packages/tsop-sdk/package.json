{
  "name": "tsop-sdk",
  "version": "0.1.0",
  "type": "module",
  "description": "TSOP SDK: deploy, call, and interact with compiled smart contracts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "files": ["dist"],
  "dependencies": {
    "@bsv/sdk": "^2.0.5",
    "tsop-lang": "workspace:*",
    "tsop-ir-schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
